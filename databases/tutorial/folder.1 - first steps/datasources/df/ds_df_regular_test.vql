# REQUIRES-PROPERTIES-FILE - # Do not remove this comment!
CREATE OR REPLACE DATASOURCE DF ds_df_regular_test
    FOLDER = '/1 - first steps'
    ROUTE LOCAL 'LocalConnection' '${databases.tutorial.folder.1 - first steps.datasources.df.ds_df_regular_test.ROUTE.LOCAL.PATH}' FILENAMEPATTERN = ''
    COLUMNDELIMITER = '-'
    ENDOFLINEDELIMITER = '\n'
    HEADER = TRUE;

